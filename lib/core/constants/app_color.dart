import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const Color primary = Color(0xFF4B4CED);
  static const Color secondary = Color(0xFF37B6E9);

  // Dark Colors
  static const Color background = Color(0xFF222834);
  static const Color surface = Color(0xFF242C3B);
  static const Color dark = Color(0xFF353F54);

  // Basic Colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
}
